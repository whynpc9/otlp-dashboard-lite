{
  "name": "@devdash/cli",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "devdash": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "start": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@devdash/server": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^3.1.4"
  }
}
